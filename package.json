{
  "name": "the-construct",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "dev": "wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.129.0"
  }
}
